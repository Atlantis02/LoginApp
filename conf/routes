# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /registerUser               controllers.Application.registerUser()
GET 	/loginUser					controllers.Application.loginUser()
GET 	/validateUser				controllers.Application.validateUser()
GET 	/saveUserProfile			controllers.Application.saveUserProfile()
GET 	/resendValidationMail		controllers.Application.resendValidationMail()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
